{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter './packages/*' build",
    "test": "vitest run",
    "test:compliance": "vitest run --project compliance",
    "lint": "biome check .",
    "lint:fix": "biome check --fix .",
    "typecheck": "tsc --build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "@moqtap/codec": "workspace:*",
    "tsup": "^8.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  },
  "name": "",
  "version": ""
}
